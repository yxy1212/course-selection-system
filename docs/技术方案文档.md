# 选课管理系统 - 技术方案文档

## 1. 系统架构

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Vue2      │────▶│  SpringBoot │────▶│   MySQL8    │
│   前端       │     │   后端 API   │     │   数据库     │
└─────────────┘     └─────────────┘     └─────────────┘
```

## 2. 技术选型

### 2.1 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.2.x | Web 框架 |
| Java | 17 | 运行时 |
| MyBatis Plus | 3.5.x | ORM |
| MySQL Driver | 8.x | 数据库连接 |
| JWT | 0.11.x | 认证 |
| Lombok | 1.18.x | 简化代码 |

### 2.2 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 2.7.x | UI 框架 |
| Vite | 5.x | 构建工具 |
| Axios | 1.x | HTTP 客户端 |
| Vue Router | 3.x | 路由 |

### 2.3 数据库

| 表名 | 说明 |
|------|------|
| user | 用户表 |
| course | 课程表 |
| course_selection | 选课记录表 |

## 3. 数据库设计

### 3.1 用户表 (user)

```sql
CREATE TABLE user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    real_name VARCHAR(50),
    role ENUM('STUDENT', 'TEACHER', 'ADMIN') DEFAULT 'STUDENT',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.2 课程表 (course)

```sql
CREATE TABLE course (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    teacher_id BIGINT NOT NULL,
    credits INT DEFAULT 0,
    capacity INT DEFAULT 50,
    selected_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (teacher_id) REFERENCES user(id)
);
```

### 3.3 选课记录表 (course_selection)

```sql
CREATE TABLE course_selection (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id BIGINT NOT NULL,
    course_id BIGINT NOT NULL,
    selected_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (student_id) REFERENCES user(id),
    FOREIGN KEY (course_id) REFERENCES course(id),
    UNIQUE KEY uk_student_course (student_id, course_id)
);
```

## 4. API 设计

### 4.1 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 登录 |
| POST | /api/auth/register | 注册 |

### 4.2 课程

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/courses | 课程列表 |
| GET | /api/courses/{id} | 课程详情 |
| POST | /api/courses | 创建课程 (教师) |
| PUT | /api/courses/{id} | 更新课程 (教师) |
| DELETE | /api/courses/{id} | 删除课程 (教师) |

### 4.3 选课

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/selections/{courseId} | 选课 |
| DELETE | /api/selections/{courseId} | 退课 |
| GET | /api/selections/my | 我的选课 |

## 5. 部署方案

### 5.1 Docker Compose

- MySQL: 3306
- Backend: 8080
- Frontend: 80 (Nginx)

### 5.2 环境变量

- MYSQL_ROOT_PASSWORD
- JWT_SECRET
- SERVER_PORT
