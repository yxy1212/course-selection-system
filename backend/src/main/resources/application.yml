server:
  port: 8080

spring:
  application:
    name: course-selection-system
  datasource:
    url: jdbc:mysql://localhost:3306/course_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: course-system-secret-key-2024-this-is-a-very-long-secret-key-for-jwt
  expiration: 86400000
